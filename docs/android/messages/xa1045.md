---
title: .NET for Android error XA1045
description: XA1045 error code
ms.date: 09/22/2026
f1_keywords:
  - "XA1045"
---

# .NET for Android error XA1045

## Example messages

```
error XA1045: Input file `/path/to/jni-remapping.xml` does not start with `<replacements/>`.
```

## Issue

The generated JNI remapping XML file named in the error does not have the
expected `<replacements/>` root element. The build creates this file by merging
its internal remapping inputs before generating native JNI remapping code.

## Solution

Inspect the file path reported in the error and the build output to identify
which workload or package remapping input produced invalid XML. Each remapping
input must start with a `<replacements>` root element. For example:

```xml
<replacements>
  <replace-type from="com/example/Old" to="com/example/New" />
</replacements>
```

After correcting or updating the input's source package, delete the project's
`obj` directory and rebuild so the merged remapping file is regenerated.
